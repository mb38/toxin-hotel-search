mixin checkbox(args)
    label.checkbox
        input.checkbox__input(type="checkbox" name="checkbox")&attributes(attributes)
        span.checkbox__checkmark
            .checkbox__check
        .checkbox__text #{args.text}